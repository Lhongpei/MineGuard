# 企业填报智能体部署与运维

本页用于把企业端从开发终端中的前台命令，转换为可重启、可监控、数据路径固定的
Linux 服务。企业端和监管平台仍是两个独立部署单元；本服务只持企业账号、草稿库
和平台运输凭证，不应获得监管数据库或来源设备签名密钥。

## 1. 目录与账号

下面使用独立低权限账号和固定绝对路径：

```bash
sudo useradd --system --home /var/lib/enterprise-agent \
  --shell /usr/sbin/nologin enterprise-agent
sudo install -d -m 0755 -o root -g root /opt/enterprise-agent
sudo install -d -m 0750 -o root -g enterprise-agent /etc/enterprise-agent
```

把仓库部署到 `/opt/enterprise-agent`，在 `agent/` 内创建虚拟环境并安装：

```bash
cd /opt/enterprise-agent/agent
python3 -m venv .venv
.venv/bin/python -m pip install -e .
.venv/bin/enterprise-agent --version
.venv/bin/enterprise-agent --help
```

代码和虚拟环境由发布账号管理，运行账号不应拥有写权限。草稿、审计和提交记录只
写入 `/var/lib/enterprise-agent`。

## 2. 环境文件

复制仓库中的安全字段模板，不要直接复用示例值：

```bash
sudo install -m 0640 -o root -g enterprise-agent \
  deploy/enterprise-agent.env.example \
  /etc/enterprise-agent/enterprise-agent.env
```

使用交互命令生成用户密码摘要：

```bash
sudo -u enterprise-agent \
  /opt/enterprise-agent/agent/.venv/bin/enterprise-agent hash-password
```

然后编辑环境文件中的单行 `ENTERPRISE_AGENT_USERS_JSON`。文件必须保持
`root:enterprise-agent`、`0640`；不得写入明文密码、来源网关 HMAC 密钥或真实
原始材料。平台运输 HMAC 与来源 HMAC 是两类密钥，企业智能体只能持前者。

生产平台地址必须是 HTTPS。企业页面若也由 HTTPS 反向代理提供，应设置
`ENTERPRISE_AGENT_SECURE_COOKIE=true`，并把
`ENTERPRISE_AGENT_PUBLIC_ORIGIN` 设置为浏览器实际使用的唯一 HTTPS origin，
例如 `https://report.enterprise.example`。该值不能含路径、查询、片段或账号信息；
非标准端口必须显式包含。若只是单机或 SSH 隧道上的临时 HTTP 验收，应移除
public origin 并保持 Secure Cookie 为 `false`，否则浏览器不会通过 HTTP 发回
Secure Cookie。

### 2.1 环境变量完整清单

服务不会自动加载 `.env`。下表是当前 `Settings.from_environment()` 实际读取的
全部变量；未列出的变量不会影响本服务。修改任何变量后都要重启进程。

| 变量 | 默认值或范围 | 运维含义 |
| --- | --- | --- |
| `ENTERPRISE_AGENT_DB` | `./data/enterprise-agent.db` | 生产必须改为状态目录内的绝对路径 |
| `ENTERPRISE_AGENT_HOST` | `127.0.0.1` | 建议保持回环，由 HTTPS 代理接入 |
| `ENTERPRISE_AGENT_PORT` | `8090`，1–65535 | 后端监听端口 |
| `ENTERPRISE_AGENT_USERS_JSON` | 未设置 | 单行逐用户配置；生产必配 |
| `ENTERPRISE_AGENT_ALLOW_ANONYMOUS_LOCAL` | `false` | 仅限回环开发调试 |
| `ENTERPRISE_AGENT_SESSION_TTL_SECONDS` | `28800`，300–604800 | 会话有效期 |
| `ENTERPRISE_AGENT_SECURE_COOKIE` | `false` | HTTPS 生产环境设为 `true` |
| `ENTERPRISE_AGENT_PUBLIC_ORIGIN` | 未设置 | 精确 HTTP(S) origin，不含路径、查询和账号 |
| `AGENT_V2_ENABLED` | `true` | Agent V2 耐久只读任务总开关 |
| `AGENT_V2_SCHEDULER_ENABLED` | `true` | 每日/间隔计划后台轮询开关 |
| `AGENT_V2_SCHEDULER_POLL_SECONDS` | `5`，0.25–60 | 到期计划轮询间隔 |
| `AGENT_V2_WORKER_COUNT` | `2`，1–8 | V2 任务工作线程数 |
| `AGENT_V2_SPECIALIST_WORKER_COUNT` | `4`，1–8 | 四专家并行线程池上限 |
| `AGENT_V2_FLOW_LEASE_SECONDS` | `120`，30–600 | 多实例执行租约、续租和未授权派发回收窗口 |
| `PLATFORM_BASE_URL` | 未设置 | 监管平台根地址，生产应为 HTTPS |
| `PLATFORM_CLIENT_ID` | 未设置 | 监管端登记的企业客户端 |
| `PLATFORM_TRANSPORT_HMAC_SECRET` | 未设置，至少 32 字节 | 企业报送运输密钥，不是来源密钥 |
| `PLATFORM_SUBMISSION_PATH` | `/v1/enterprise-submissions` | 提交路径 |
| `PLATFORM_CAPABILITIES_PATH` | `/v1/enterprise-submission-capabilities` | 能力协商路径 |
| `PLATFORM_BEARER_TOKEN` | 未设置 | 仅监管端明确签发第二因子时配置 |
| `PLATFORM_TIMEOUT_SECONDS` | `20`，1–120 | 监管平台请求超时 |
| `DEEPSEEK_API_KEY` | 未设置 | 缺省为规则模式；只能由密钥系统注入 |
| `DEEPSEEK_BASE_URL` | `https://api.deepseek.com` | OpenAI-compatible 根地址 |
| `DEEPSEEK_MODEL` | `deepseek-v4-flash` | 管理员批准的模型名 |
| `DEEPSEEK_TIMEOUT_SECONDS` | `20`，1–120 | 模型请求超时 |
| `DEEPSEEK_MAX_RETRIES` | `2`，0–5 | 模型重试次数 |
| `COAL_NEWS_SEARCH_ENABLED` | `true` | 新闻检索总开关 |
| `COAL_NEWS_SEARCH_TIMEOUT_SECONDS` | `25`，3–60 | 单次新闻检索总超时 |
| `COAL_NEWS_BAIDU_ENABLED` | `true` | 百度新闻优先通道 |
| `COAL_NEWS_BAIDU_TIMEOUT_SECONDS` | `3`，1–10 | 百度通道超时 |
| `COAL_NEWS_DEEPSEEK_WEB_SEARCH_ENABLED` | `true` | DeepSeek Web Search 后备 |
| `COAL_NEWS_DEEPSEEK_TIMEOUT_SECONDS` | `24`，3–60 | DeepSeek 搜索超时 |
| `COAL_NEWS_BING_FALLBACK_ENABLED` | `false` | Bing RSS 最后后备 |
| `COAL_NEWS_SEARCH_CACHE_TTL_SECONDS` | `300`，30–3600 | 新闻缓存秒数 |
| `COAL_NEWS_SEARCH_MAX_RESULTS` | `8`，1–20 | 单次来源上限 |
| `COAL_NEWS_SEARCH_MAX_RESPONSE_BYTES` | `1048576`，65536–2097152 | 搜索响应上限 |
| `COAL_NEWS_SEARCH_MAX_CONCURRENCY` | `4`，1–8 | 新闻请求并发上限 |

只要配置了任一平台核心项，就必须同时提供
`PLATFORM_BASE_URL`、`PLATFORM_CLIENT_ID` 和
`PLATFORM_TRANSPORT_HMAC_SECRET`。Agent V2 每日体检不使用模型或平台运输配置；
新闻联网状态也不会影响本地体检。

完整 V2 参数、调度 API 和安全边界见
[煤炭 Agent V2 耐久任务流与治理使用说明](煤炭Agent-V2任务流.md)。

## 3. systemd

复制并核对单元文件中的安装路径、账号和端口：

```bash
sudo install -m 0644 -o root -g root \
  deploy/enterprise-agent.service.example \
  /etc/systemd/system/enterprise-agent.service
sudo systemctl daemon-reload
sudo systemctl enable --now enterprise-agent
```

示例单元的关键约束：

- `WorkingDirectory` 固定代码目录；数据库仍使用环境文件中的绝对路径；
- `EnvironmentFile` 由 root 管理，应用日志不会回显其中的密钥；
- `Restart=on-failure` 处理异常退出，但正常人工停止不会自动拉起；
- `UMask=0077` 限制新建数据库及临时文件权限；
- `ProtectSystem=strict` 把系统和应用目录设为只读；
- `StateDirectory` 与 `ReadWritePaths` 只开放企业状态目录；
- `NoNewPrivileges`、空 capability 集及其他 systemd 沙箱项降低进程权限；
- `KillMode=control-group` 保证停止服务时一并清理其子进程。

服务只监听 `127.0.0.1:8090`。正式多人访问应由企业内网 HTTPS 反向代理连接该
回环端口；临时远程验收可使用 SSH 隧道，不要直接对局域网暴露 8090。

## 4. HTTPS 反向代理

仓库提供 [Nginx 示例](../deploy/nginx-enterprise-agent.conf.example)。域名与证书
必须换成本单位值，并与环境文件保持一致：

```text
ENTERPRISE_AGENT_PUBLIC_ORIGIN=https://report.enterprise.example
proxy_set_header Host report.enterprise.example;
```

代理必须保留这个精确 Host authority，浏览器发送的 Origin 也必须与 public
origin 精确相同。应用不会读取或信任 `X-Forwarded-Host`、
`X-Forwarded-Proto`、`X-Forwarded-For` 来放宽 Host/Origin 校验；真实客户端
地址以受保护的反向代理日志为准。防火墙只向用户开放代理的 443，不开放 8090。

应用连接看到的是代理的 `127.0.0.1`，因此应用内置的登录失败保护只是第二道边界，
不能代替按真实客户端 IP 的边缘限速；多名用户还会共享这一应用侧地址预算。
Nginx 示例只对精确的 `/api/v1/auth/login` 设置独立 `limit_req`，不会误伤 health、
草稿保存或正式提交。代理必须直接看到真实客户端 IP；若它前面还有受信负载均衡器，
应按本单位标准只信任明确上游并正确配置 Nginx real-ip 模块，绝不能无条件信任
任意客户端提供的 X-Forwarded-For。

示例 access log 记录真实连接地址、方法、规范化路径、状态和耗时，不记录查询串、
Cookie、CSRF、请求体或响应体。若接入单位的统一日志平台覆盖该格式，也必须继续
排除认证头和报送内容，并对日志读取设置权限和留存周期。

配置后先在服务器本机检查后端 health，再通过正式域名登录并创建一个测试草稿。
若返回 `host_not_allowed`，核对 Nginx 的 `Host` 与 public origin 的 authority；
若返回 `cross_origin_request_denied`，核对浏览器 URL、HTTPS 与端口是否完全一致。

## 5. 启停、日志与健康检查

```bash
sudo systemctl status enterprise-agent
sudo systemctl restart enterprise-agent
sudo systemctl stop enterprise-agent
sudo journalctl -u enterprise-agent -n 200 --no-pager
sudo journalctl -u enterprise-agent -f
curl -fsS http://127.0.0.1:8090/api/v1/health
```

health 返回 `status=ok` 说明企业 HTTP 服务可响应；
`platform_configured=true` 只说明三项平台配置已装载，不代表监管平台当前在线。
`harness_available=true` 表示受控任务运行时已加载，
`tool_calling_mode=llm|deterministic` 表示当前是模型规划还是纯本地确定性模式；
`agent_v2_available=true` 表示耐久任务运行时已加载，
`agent_v2_scheduler_enabled=true` 表示每日/间隔计划会后台轮询，
`agent_v2_governed_learning=proposal_approval_only` 表示当前只有提案审批制，
不是自动自我修改。上述字段都不表示某一具体任务已完成。正式监控还应单独检测
监管 capabilities、HTTPS 证书有效期、系统时钟、磁盘剩余空间、V2
`blocked/failed` 数量、计划 `last_error`、待审批数量和最近一次备份。

日志正常情况下只包含请求路径、状态码和启动信息，不应包含 Cookie、请求体、
密码或 HMAC。若出现提交失败，可按错误码排查，但不要把环境文件或完整报送包贴入
工单和聊天。

## 6. 数据、重启与备份

SQLite 草稿、原始材料摘要、人工核对、回执和审计数据是明文静态存储；应用只通过
系统账号、目录权限和 `UMask` 限制访问，没有宣称数据库本身已加密。生产主机必须
对状态盘或卷启用单位认可的静态加密与密钥托管，备份介质同样加密，并对
`/var/lib/enterprise-agent`、备份读取和恢复操作保留访问审计。

SQLite 主文件运行时可能同时存在 `-wal` 和 `-shm`。不要在服务运行中只复制单个
`.db` 文件。当前企业端没有内置在线备份命令；部署单位应选用以下一种受控方式：

1. 在维护窗口停止服务，确认端口已关闭后备份整个
   `/var/lib/enterprise-agent`，完成后再启动；
2. 使用经过验证的 SQLite 在线备份工具，并在恢复演练中验证草稿、提交记录及
   审计链。

备份必须使用独立的最小权限凭证，设置留存与销毁周期，并通过加密传输送达备份
系统。恢复时先完成审批和审计登记，停止服务，恢复到权限为
`enterprise-agent:enterprise-agent`、目录模式 `0700` 的状态目录，再启动并检查：

- 草稿和回执数量符合备份时点；
- 已成功提交的草稿仍保持锁定；
- 失败提交保留原幂等键和原请求，可在平台恢复后重试；
- 每个重要草稿的审计完整性仍为有效。
- 智能任务、工具调用、批准记录和任务事件链均可读取，过程链完整性仍为有效；
- V2 的 `agent_flows`、`agent_flow_steps`、`agent_flow_events` 可读取，任务事件链
  完整性有效；
- `agent_jobs`、`agent_job_events`、`agent_trigger_events` 仍保留计划、下次运行
  时间、幂等事件和计划事件链；
- `agent_memory_proposals`、`agent_memories`、`agent_skill_proposals`、
  `agent_skill_versions` 的审批人、来源、版本和记录摘要一致；
- 中断时正在执行的写动作若被标记为“结果未知”，已由人员对照草稿修订和审计记录
  完成人工处置，不能直接重放。

V2 正在运行的任务在服务恢复时会把旧运行步骤记为
`failed / flow_interrupted`，未取消任务增加一次 `attempt` 并重新排队。该自动
重算只适用于 V2 本地只读工作流；确认、提交和其他外部副作用动作绝不能按此规则
重放。调度器会为“已启用但没有 `next_run_at`”的每日/间隔计划重新计算下次时间，
但不会承诺补跑停机期间的每一个历史时点。

更换 `ENTERPRISE_AGENT_DB`、`WorkingDirectory` 或运行账号前必须显式迁移数据，
否则“重启后数据丢失”通常只是进程打开了另一份相对路径数据库。

## 7. 发布前验收

从仓库根目录运行完全独立的黑盒验收：

```bash
python3 scripts/verify_two_process.py
```

该脚本使用临时目录，不接触生产数据库或真实密钥，并验证正常链路、权限拒绝、
配置缺失、错误运输密钥、端口占用、平台停机恢复、幂等重试、双端重启持久性及
退出清理；还会经公开 HTTP 接口运行一次无模型煤炭体检并检查工具目录、预算和
任务事件链。发布升级后应同时执行企业端测试和合同校验。

升级前不要遗留待批准的写工具。工具参数摘要同时绑定当前 Harness 和工具定义；
若版本变化，旧批准应失败关闭，由创建人重新发起任务并核对新动作。任务列表只能
返回摘要，完整参数和结果只允许创建人从任务详情读取。发现事件链、参数摘要或结果
摘要校验失败时，应立即停止批准动作、保全数据库副本并进入安全事件处置，不要通过
直接改库把状态改成“成功”。

任务事件 SHA-256 链和同库链头可以发现意外损坏、尾部截断及没有同步更新事件的
局部改库，但不是外部数字签名，不能防住有权同时改写整库并重算摘要的主机管理员。
如需更强不可抵赖性，应把链头定期发送到单位的外部只写审计系统或时间戳服务；该
外部锚定不应与企业草稿数据库共用写权限。

## 8. Agent V2 运维与自动数据边界

### 8.1 资源与调度

生产初期建议保留默认的 2 个任务线程和 4 个专家线程，观察 CPU、SQLite 写等待和
草稿规模后再调整。单账号最多 50 个未删除计划，单实例最多 1000 个。间隔计划最短
5 分钟；不要用大量短周期计划代替真正的数据到达事件。

调度器先创建 `dispatch_ready=false` 的持久化流，只有计划修订、配置摘要、任务关联
和两条审计链在同一 SQLite 事务内确认后才允许工作线程领取。稳定人工请求和业务事件
在这个极短窗口中断后，会按原请求材料、事件进度和完整性锚点继续挂接；从未执行且被
孤儿回收的流也只能由同一幂等请求在事务内复活。调度器会主动续跑未完成事件；损坏或
长期不可达事件最多保留未派发流 24 小时。排队和运行配额只统计审计可验证的任务，
旧版无控制锚点记录不会挤占正常容量，也不会被恢复线程反复派发。

每个内置工具还有 30 秒以内的工作流硬截止，并通过最多 64 个有界守护隔离线程保证
阻塞工具不妨碍进程退出。Python 仍不能在进程内安全强杀任意第三方代码，因此未来
接入数据库驱动或厂商 SDK 时，适配器本身仍必须配置连接/读取超时、响应上限和可取消
I/O；不得只依赖工作流外层截止。当前内置 V2 工具均为本地、有界、只读实现。

精简容器若使用 `Asia/Shanghai` 等 IANA 时区，应安装 `tzdata`。关闭
`AGENT_V2_SCHEDULER_ENABLED` 只停止后台到期轮询，手工任务仍可运行；关闭
`AGENT_V2_ENABLED` 才会使全部 V2 路由不可用。

### 8.2 常见故障

| 现象 | 优先检查 | 处置 |
| --- | --- | --- |
| V2 API 返回 503 | `AGENT_V2_ENABLED`、是否仍访问旧进程 | 改配置后重启，核对 health |
| 任务长期 `queued` | 工作线程、数据库路径、磁盘和目录权限；查看 `dispatch_ready` | 可派发任务会从 SQLite 恢复；未授权派发超过租约窗口会安全取消，不执行工具 |
| `blocked / draft_unavailable` | 计划绑定草稿是否仍存在 | 恢复草稿条件后由创建人重试 |
| `flow_integrity_failed` | 数据库和事件链是否被改写/截断 | 立即停止使用结论并保全副本，不要直接改库 |
| 每日计划不运行 | scheduler health、`enabled`、`next_run_at`、时区 | 修正配置、安装 tzdata、查看计划事件 |
| 事件接收但无任务 | 账号、事件名、草稿编号和计划启用状态 | 查看事件返回的 `triggered` 和计划 `last_error` |
| 记忆治理 403/409 | `governance_review`、修订号或四眼审批人 | 刷新后使用另一名有该权限的实名账号 |
| 技能治理 403/409 | `skill_admin`、修订号或四眼审批人 | 刷新后使用另一名有该权限的实名账号 |
| 技能批准后不能运行 | `runtime_activation` | `approved_inactive` 是设计状态，需代码发布和显式注册 |

### 8.3 与自动抓数、edge-agent 的关系

网页中的“让 Agent 从材料自动填入”可以创建或复用未提交草稿，并把用户主动提供的
合同 JSON、CSV 确定性写入草稿；它不是后台自动抓数。Agent V2 只检查企业端已经
存在的草稿，不会自动登录 ERP/MES、定时创建下一期草稿或抓取矿端设备。
`edge-agent/` 使用自己的合同和运输密钥直接向监管平台发送只读遥测，不共享企业
Agent 数据库，也不会自动触发企业任务。

若部署目标是“数据到齐即体检”，还需要独立上游适配器完成获准数据读取、来源签名、
草稿导入和幂等事件发送。当前事件 API 使用企业登录会话和 CSRF，仓库没有通用机器
账号认证或爬虫。生产集成应新增最小权限、可轮换的服务间签名契约；不得把浏览器
密码、Cookie、CSRF 或来源 HMAC 固化进脚本。

无论如何集成，V2 都不得获得企业人工确认、监管提交、来源补签或设备控制能力。

### 8.4 旧库升级与隔离

首次打开旧 SQLite 库时，结构迁移在跨进程独占事务内完成；多实例同时启动只会有一个
实例执行迁移，其他实例等待后重读结构。高于当前程序支持版本的数据库会在任何写入和
WAL 模式切换前被拒绝，禁止旧程序降级打开。升级前仍应备份整个状态目录。

旧记录如果缺少当前版本要求的内容摘要、父审批绑定、事件链锚点或流控制摘要，会按行
失败关闭：指定编号读取或变更会被拒绝，普通治理列表和容量统计会跳过这些隔离记录，
同键可以通过新提案重新建立可核验版本。不要直接 SQL 补哈希、改状态或拼事件；应保全
原库供取证，使用新提案重建，或由经过批准的离线迁移程序生成可审计映射。Schema v4
会在升级表上补充写入守卫，使状态枚举、布尔值、正修订号、治理父提案和版本唯一性等
后续写入与全新数据库采用同一失败关闭边界；它不会修改或信任迁移前的隔离行。

生产接入真实 ERP/MES、衡器或煤质系统后，应先做影子运行：只读取、签名、导入和触发
体检，不自动确认或提交；把 Agent 结论与人工台账并行比对至少一个完整业务周期，再
按误报、漏报、延迟和来源完整率决定是否扩大范围。
